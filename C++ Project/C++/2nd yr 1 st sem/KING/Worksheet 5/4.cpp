#include<iostream.h>
int Fib(int v);
void main()
{
	int n;
	cout<<"Enter the number:\t";
	cin>>n;
	
	cout<<"\nThe Fibonacci is "<<Fib(n);
}
int Fib(int v)
{
	int s=0;
	if (v <= 0) return sum;
	else
	{
		sum+=v;
		Fib(v - 1);
		}
}
